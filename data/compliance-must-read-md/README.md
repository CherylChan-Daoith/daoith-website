# 合规解决方案必读文件（Dify 上传包）

上传目标知识库：`合规解决方案必读文件`（dataset id: `5038c599-48c6-4b90-ab62-b41cf7b59cb8`）

## 本目录维护什么

| 文件 | 用途 | 说明 |
|------|------|------|
| `00-导读.md` / `00-必须知道的知识点.md` | 生成前总则 / 架构 | 可从此目录上传 |
| `15-出报告硬约束与路径要点.md` | 报告版式、路由、路径 A/B/C 要点（原提示词细则） | **必传**；配合精简版系统提示词 |
| `10–14-附件*.md` | 跨境电商实操附件 | 可从此目录上传 |
| `20–22-方案样本-路径*.md` | 诊断报告结构与口径样本 | 可从此目录上传 |
| **问题1–7** | 按问卷步骤的分篇规则 | **不在本目录维护**；以 Dify 知识库内最新「问题X+关键词」为准 |

请勿再上传旧版 `01-问题1.md`～`07-问题7.md`（无关键词后缀），以免覆盖知识库新版。

## 上传命令示例（不含问题1–7）

```bash
DIFY_DATASET_API_KEY=dataset-xxx \
  .venv-pdf/bin/python scripts/dify_upload_md.py \
  --dir data/compliance-must-read-md \
  --dataset-id 5038c599-48c6-4b90-ab62-b41cf7b59cb8 \
  --replace
```

诊断助手第八步出报告流水线：通读 `15`（要求）→ 通读 `00-必须知道的知识点`（知识基础）→ 按各题答案检索「问题X+关键词」注意事项 → 判定路径 → 参考对应方案样本 → 生成报告。
